{
	"GetTotalPages Sicoco": {
		"prefix": "si-totalpages",
		"body": ["getTotalPages(${1:totalData}, ${2:perPage})"],
		"description": "getTotalPages(totalData: number, perPage: number | string): number"
	},
	"HandleInfiniteScroll Sicoco": {
		"prefix": "si-infinite",
		"body": ["handleInfiniteScroll(${1:HTMLElement}, ${2:loadMoreFn})"],
		"description": "handleInfiniteScroll(container: HTMLElement, loadMoreFn: () => void)"
	}
}
